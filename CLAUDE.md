# Pluris — Claude Code project instructions

The full, authoritative agent rules for this repo live in `AGENTS.md` (shared with every
coding agent). They are imported below so Claude Code loads them automatically. Do not
duplicate rules here — edit `AGENTS.md` instead.

@AGENTS.md
