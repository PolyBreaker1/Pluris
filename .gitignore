/bin/
/dist/
*.exe

# Templ-generated code (regenerated by `make gen`)
**/*_templ.go

# Local dev data
/deploy/data/
/deploy/certs/*.pem
/deploy/certs/*.key
/deploy/certs/*.crt

# Database files (generated at runtime)
*.db
*.db-shm
*.db-wal
pluris.db*

# Built binaries in root (should be in bin/)
/pluris-console

# Environment
.env
.env.local

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store

go.work.sum

# Private planning docs (owner-only, not for the public repo)
/docs/funding/
