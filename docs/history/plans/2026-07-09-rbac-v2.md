# RBAC v2 Implementation Plan (inheritance, group roles, standardized UX, full-page create)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** GLPI-style role inheritance + group-role assignment + standardized role/dependency-group list UX + full-page user create, per `docs/superpowers/specs/2026-07-09-rbac-v2-design.md` (THE requirements contract — read it per task; do not invent).

**Tech:** Go 1.25, Echo v4, Templ, sqlc/SQLite, vanilla JS.

## Global Constraints
Same as the previous plan, all binding: never touch `pluris.db*`; t.TempDir() tests; `-buildvcs=false`; `make gen`/`sqlc generate` (ASCII SQL comments, never hand-edit generated); migration 005 append-only, no PRAGMA; no new deps; owner owns git — NO commits, "Commit" steps = STOP and report; suite green + gofmt clean per task; DetailShell/INV-L/resolveTenant* patterns; do NOT edit `.superpowers/sdd/progress.md`; NEVER run `git checkout/restore/stash`; implementers work alone (no sub-subagents).

## Shared reference (frozen names)
```go
// pkg/authz additions (Task 2)
func (s *Service) ResolveRoleMatrix(ctx context.Context, role db.Role) (Grants, error) // inheritance-resolved effective matrix
func (s *Service) SaveRoleOverrides(ctx, roleID int64, full Grants) error              // stores diff vs parent-effective; full matrix for parentless
func (s *Service) SetRoleParent(ctx, roleID int64, parentID int64) error               // 0 clears; cycle+depth+builtin-guarded; ErrRoleCycle / ErrBuiltinParent
var ErrRoleCycle, ErrBuiltinParent error
const MaxRoleDepth = 5
// catalog/permissions (Task 2): func BuiltinSlugs() []string  (single source; services+authz consume)
// Task 3: EffectiveGrants unions direct + group roles (each ResolveRoleMatrix'd)
// db (Task 1): group_roles queries AssignRoleToGroup/RemoveRoleFromGroup/ListRolesForGroup/ListGroupsForRole/ListGroupRolesForIdentity (join group_memberships->group_roles->roles by identity), SetRoleParent(UpdateRoleParent), ListRoleChildren
// templates (Task 6): roleOriginBadge, parent-chain hero helper; (Task 7) rolePickerOptions(roles, depth-aware) shared by user+group tabs
```

---

### Task 1: Migration 005 + queries
**Files:** Create `db/schema/005_role_hierarchy_group_roles.sql`; modify `pkg/database/database.go` (migrations slice), `db/queries/roles.sql` (+group_roles + parent queries); test `pkg/database/role_hierarchy_test.go`.
- [ ] Failing test: scratch DB round-trip — set parent via `UpdateRoleParent`, read back; assign role to group via `AssignRoleToGroup`, `ListRolesForGroup` returns it; `ListGroupRolesForIdentity` returns roles of groups the identity is a member of (create tenant, group, identity, membership via existing queries — grep group_memberships insert query name).
- [ ] Migration per spec §1 (ALTER roles + CREATE group_roles + indexes on group_roles(group_id), (role_id)); register in database.go; queries: `UpdateRoleParent :exec` (SET parent_role_id nullable via sqlc.narg or two queries — check how other nullable updates are written; simplest: `UPDATE roles SET parent_role_id = @parent_role_id ...` with sql.NullInt64), `AssignRoleToGroup :exec` (INSERT OR IGNORE), `RemoveRoleFromGroup :exec`, `ListRolesForGroup :many` (join roles), `ListGroupsForRole :many` (join groups, return id+name), `ListGroupRolesForIdentity :many` (`SELECT r.* FROM roles r JOIN group_roles gr ON gr.role_id=r.id JOIN group_memberships gm ON gm.group_id=gr.group_id WHERE gm.<member-col>=@identity_id` — READ db/schema/001 group_memberships columns first: it likely has member_type/member_id or identity_id; filter identity members correctly), `ListRoleChildren :many` (WHERE parent_role_id=@id). `sqlc generate`.
- [ ] Tests pass; full suite; gofmt. STOP/report.

### Task 2: Inheritance resolver + BuiltinSlugs
**Files:** Modify `catalog/permissions/registry.go` (+BuiltinSlugs), `pkg/authz/service.go` (+ResolveRoleMatrix/SaveRoleOverrides/SetRoleParent + errors), `pkg/services/roles.go` + `pkg/authz/service.go` (consume BuiltinSlugs, delete local lists); tests in `pkg/authz/service_test.go`.
- [ ] Failing tests: parentless role resolves to its stored matrix; child(parent=technician, override identity.delete=yes) resolves technician's matrix + override; grandchild chain merges leaf-wins; SaveRoleOverrides on child stores ONLY diff keys (read raw permissions JSON to assert) and full matrix for parentless; SetRoleParent rejects self/cycle (A→B→A) with ErrRoleCycle, rejects builtin child with ErrBuiltinParent, rejects cross-tenant parent (not-found error), accepts 0 to clear; depth: chain of 5 ok, setting a 6th rejects; resolve is depth-capped defensively.
- [ ] Implement per spec §2. `BuiltinSlugs()` returns the 4 slugs in privilege order; refactor `pkg/services/roles.go` builtinRoles iteration and authz `EnsureBuiltinGrants` to consume it (keep names/descriptions local to roles.go — only the slug list consolidates).
- [ ] Pass; full suite; gofmt. STOP/report.

### Task 3: EffectiveGrants v2 + cache recompute
**Files:** Modify `pkg/authz/service.go` (EffectiveGrants: direct + `ListGroupRolesForIdentity`, each via ResolveRoleMatrix), `pkg/services/roles.go` (recomputeRoleCache: rank over direct+group roles; export `RecomputeForGroupMembers(ctx, groupID)` helper), `pkg/services/groups.go` + `console/handlers/groups.go` (membership add/remove → recompute that member); tests.
- [ ] Failing tests: identity in group with technician role + direct user role → EffectiveGrants includes technician grants (e.g. Can identity.create true); removing group role drops it; identities.role cache reflects highest of direct+group after group-role assign (recompute path) and after membership add/remove.
- [ ] Implement; hook recompute into group-role assign/remove (service added Task 4 calls it too — export whatever both need now).
- [ ] Pass; full suite. STOP/report.

### Task 4: Role backend v2 (create/parent/override-save/clone-in-detail/group-assign)
**Files:** Modify `console/handlers/pluris_policy.go` (+`PlurisPolicyCreate` POST /policy/pluris — name required + optional parent_id + gate manage_permissions; `PlurisPolicySetParent` POST /policy/pluris/:id/parent — cycle/builtin errors → 400 with message; PlurisPolicySave → SaveRoleOverrides + self-lockout via RESOLVED effective; keep Clone handler, list moves UI-side), NEW group-role handlers in `console/handlers/group_roles.go` (`GroupRoleAssign` POST /groups/:id/roles, `GroupRoleRemove` POST /groups/:id/roles/:roleID/remove — gate manage_role_assignments, resolveTenant group+role, recompute members, activity rows); routes; tests.
- [ ] Failing tests: create role (name only → parentless custom, empty overrides = deny-all effective; with parent_id=technician → effective == technician); set-parent cycle → 400; save on child stores diff (assert raw JSON); self-lockout still enforced using resolved grants; group assign/remove round-trip + RBAC 403 (user template) + cross-tenant 404; via-group listed by ListGroupRolesForIdentity.
- [ ] NOTE: groups routes — check whether a group detail page/routes exist (grep `/groups` in server.go); if no group detail page exists, register the two POST routes under `/groups/:id/roles` anyway (Task 7 builds the minimal group detail UI).
- [ ] Pass; full suite. STOP/report.

### Task 5: Standardized lists (roles + dependency groups toolbars, Create button)
**Files:** Modify `web/templates/pluris_policy.templ` (list page: search+quick-filter toolbar wired via `data-pluris-list`/`data-pluris-filter` attrs — MIRROR `configGroupsToolbar` in pages.templ ~655 and the pm-toolbar ~1697 EXACTLY incl. filter-attr/search-blob row attributes; add per-row `data-pp-search` blob (name+slug+type+parent) and quick filters All/Builtin/Custom; REPLACE the list-level clone form with a **Create role** button + inline panel: name input + hierarchical parent `<select>` (optgroup by template family — Task 7's shared helper; for now a flat select with indent prefixes is acceptable placeholder IF Task 7 lands the shared helper later — NO: build the shared helper HERE in `web/templates/role_picker_helpers.go` and reuse in Task 7), POST /policy/pluris; Parent column added to registry list `web/lists/pluris_roles.go`); `web/templates/dependency_groups.templ` (same toolbar treatment: search + All/Builtin/Custom quick filters); handler additions to pass parent names; `make gen`; render tests (toolbar attrs present; Create panel posts to /policy/pluris; dep-groups search input has data-pluris-filter).
- [ ] TDD: extend handler render tests first (assert `data-pluris-filter` present on both pages + "Create role" text) → implement → pass; full suite. STOP/report.

### Task 6: Role detail v2 (origin badges, parent UI, clone-in-detail)
**Files:** Modify `web/templates/pluris_policy.templ` + `pluris_policy_helpers.go`: hero parent-chain crumb + member count; General/Settings tab (name/description/parent selector for custom roles, POST .../parent); Permissions tab rows show effective value + origin badge (`own` | `inherited from <parent name>` | `default`) — handler passes both resolved effective grants AND raw own-override map + parent chain names; per-row "reset to inherited" (small button posting the full matrix with that key set to the inherited value — client-side JS: a data-inherited attr per row + tiny script resetting the control; server diff-save drops it automatically); ⋮ menu gains Clone (form with name input, existing clone endpoint); `make gen`; render tests (builtin shows no parent selector; child role shows badge text; reset control present).
- [ ] TDD render tests → implement → pass; full suite. STOP/report.

### Task 7: Group Roles tab + via-group rows + hierarchical pickers
**Files:** Check group detail existence (grep). If none: minimal group detail `web/templates/group_detail.templ` + handler + route GET /groups/:id (DetailShell: General info, Members list, Roles tab) — gated identity.view/asset.view-agnostic: use `console_access.view_roles`? NO — groups are directory objects; route-level "" (authenticated) is consistent with current groups exposure; Roles tab actions gated manage_role_assignments (Task 4 handlers). Roles tab: assigned roles table + hierarchical picker (shared `rolePickerOptions` from Task 5) + remove buttons. User detail Roles tab (`users.templ` userRolesTab): append read-only "via group <name>" rows (new query data via ListGroupRolesForIdentity + group names — extend to return group name; adjust query if needed) with disabled remove + tooltip; picker upgraded to hierarchical optgroups. `make gen`; tests: group detail 200 + testid; via-group row renders; picker optgroups present.
- [ ] TDD → implement → pass; full suite. STOP/report.

### Task 8: Full-page user create
**Files:** NEW `web/templates/user_create.templ` (+helpers if needed): create-mode page per spec §6 — all SchemaIdentity sections rendered as OPEN inputs (skip keys in `identities.NonEditableFieldKeys`; skip security section entirely; avatar placeholder non-functional pre-create), required markers on username/email, sticky action bar (Create submit + Cancel link /users), one `<form method="post" action="/users/new">` wrapping all sections + `_csrf`; MODIFY `console/handlers/handlers.go` UserNewShow (render new page) + UserCreateSubmit (accept ALL editable fields: create via identitySvc.Create with core fields, then apply the rest through the UpdateFields validation path — REUSE, don't duplicate coercion; display_name auto-fill stays; on validation error re-render create page with entered values + error banner); keep /users/:id/edit untouched; `make gen`; tests: full create with organization+contact fields persists them; missing username → 200 re-render with error + entered values preserved; non-editable key injected → refused (400 or ignored-with-error — match UpdateFields semantics); gate identity.create intact.
- [ ] TDD → implement → pass; full suite. STOP/report.

### Task 9: E2E + docs
- [ ] Full regen/build/suite/gofmt. Headless e2e (scratchpad script, scratch DB, :8096): setup/login → create role "Helpdesk L1" with parent Technician → detail shows inherited badges → override asset.delete=yes → raw JSON diff-only (via a second save idempotence check: re-open detail, effective shows yes) → create group, assign Helpdesk L1 to group → add a member → member's identities.role cache updated (fetch user detail shows role) → full-page /users/new: create user with first/last/org fields → detail reflects → dep-groups + roles list pages contain toolbar filter attrs → zero 5xx.
- [ ] Docs: HANDOFF.md new section (tasks 1-9 DONE + test names + spec/plan pointers); README ✅ line (role inheritance + group roles + standardized role management); SMALL-MODEL-TASKS: AVOID pkg/authz inheritance resolver.
- [ ] STOP/report (owner commits; browser pass).

## Self-review
Spec coverage: §1→T1, §2→T2+T3, §3 list→T5 detail→T6 pickers→T5/T7, §4→T4+T7, §5→T5, §6→T8, testing→per-task+T9. Frozen names consistent (ResolveRoleMatrix/SaveRoleOverrides/SetRoleParent/BuiltinSlugs/rolePickerOptions). Implementer-verification points named in-task (group_memberships columns, groups routes existence, nullable-parent sqlc idiom).
